{
  "name": "assets",
  "scripts": {
    "build": "bun build.ts",
    "watch": "bun build.ts --watch",
    "deploy": "bun build.ts --deploy"
  },
  "dependencies": {
    "@web-eid/web-eid-library": "^2.0.2",
    "topbar": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "esbuild": "^0.27.0"
  }
}
